// An enumeration is used for defining named constant values. An enumerated type is declared using the enum keyword.
//a simple collection of data that can not be changed
main(){
print(status);
print(status.values);
print(status.deactive);

}

enum status {
  online,
  offline,
  deactive,
  active
}